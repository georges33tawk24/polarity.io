.DS_Store
.godot/
build/
android/
*.translation

# Signing material must never enter the repo — see README "Signing".
*.keystore
*.jks
export_credentials.cfg

# Generated icon PNGs are build outputs of tools/make_icons.gd, but they are also
# referenced by project.godot and export_presets.cfg, so they DO belong in the repo.
# Listed here only to document that they are generated, not hand-drawn.

# Store screenshots are regenerated by tools/store_shots.sh and are not
# submittable yet (see DECISIONS §12y) — no point versioning them.
store/screenshots/

# Backend credentials. The anon key is public by design, but a project URL
# in a public repo is still an invitation to probe it.
supabase.cfg
